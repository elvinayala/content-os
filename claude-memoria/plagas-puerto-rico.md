---
name: plagas-puerto-rico
description: "Cliente Gilbert Torres / Plagas Puerto Rico — agente interno de IA (WhatsApp por QR + dashboard propio); repo, stack, estado (M0-M5 hechos), decisiones y pendientes"
metadata: 
  node_type: memory
  type: project
  originSessionId: a6aef734-d270-468a-9a4d-641cc7ae1ce9
  modified: 2026-09-12T13:21:54.713Z
---

**Cliente:** Gilbert Kristian Torres, **Plagas Puerto Rico** (control de plagas residencial/comercial/industrial, toda la isla). Contacto técnico: **Jafet**. Vendido por AI Borinquen: **$2,000 implementación + $147/mes soporte** tras 30 días. Onboarding: Ana (Ana Milena) + Juan David (dev).

**Qué es:** agente de IA **interno** (no atiende clientes finales): preguntas del negocio, **reportes de servicio desde fotos/videos** por WhatsApp, **cotizaciones** con precios del documento máster. Fuera de alcance: clientes finales, FB/IG, llamadas; CRM por login directo solo se evalúa (M6 = desarrollo adicional).

**Repo:** `~/Documents/Claude/Projects/plagas-puerto-rico/` (plantilla "agente-interno", multi-org). Plan: `~/.claude/plans/liente-gilbert-kristian-torres-quiet-clock.md`. README con deploy.
Stack: Next 16 + Drizzle/Postgres + R2 (fallback disco) + Anthropic (sonnet-5 chat, opus-5 reportes/cotizaciones con json_schema) + **Evolution API v2.3.7** (WhatsApp por QR, servicio aparte en Railway) + Chromium/playwright-core para PDF + ffmpeg-static para frames. Auth propia (sesión en DB, invitaciones por link, roles). Runner de jobs en Postgres desde `instrumentation.ts`.

**Estado 3-sep-2026:** M0–M5 **hechos y verificados en local** (login, documentos → prompt con cache, chat web SSE, WhatsApp con mock de Evolution incl. ráfagas de fotos, reporte PDF con fotos+video, cotización PDF con PENDIENTES y tope de descuento, panel /uso con costo/latencia/jobs y alerta diaria). Costos medidos: chat ≈ $0.01-0.03/turno, reporte ≈ $0.05-0.10, cotización ≈ $0.05. **Desplegado 3-sep-2026 (noche) en Railway**, proyecto `plagas-puerto-rico` (workspace "elvinayala's Projects"): app en https://app-production-c4e7.up.railway.app, Postgres, servicio `evolution` v2.3.7 solo por red privada (dominio público borrado), volúmenes. CLI: `npx railway` desde el repo (está enlazado; login ya hecho vía ~/.railway). Admin de prod: elvin@levelupmediapr.net (contraseña temporal entregada en chat; cambiar en /cuenta). Instancia de WhatsApp creada y esperando escaneo del QR. 4-sep: branding PlagasPR.com aplicado, **documento máster de PRUEBA** cargado en prod (`docs/master-prueba-plagaspr.md`, precios ficticios) y prueba end-to-end en prod OK (pregunta con recargos, cotización 12 meses con reglas, reporte con fotos). Guía de uso en `docs/guia-uso-equipo.md` (+ `scripts/md-a-pdf.mjs` para PDFs con marca). 5-sep: **importación de sitio web** (Documentos → Importar sitio web; crawler Chromium, mismo dominio, ≤60 páginas; tool `importar_sitio_web` para admins; regla: el máster manda sobre la web) y plagaspr.com importado en prod (46 páginas, ~50k tokens). Invitaciones admin creadas para Kristian (plagaspuertorico@gmail.com) y Japhet (japhet.alvarez1@gmail.com). Pulidos: notas de voz explicadas, chequeo de conexión WA cada 5 min con aviso, banner de desconexión. 6-sep: **Facturación** (tab en la plataforma, `/facturacion`): Stripe Checkout + Customer Portal + webhook firmado (`/api/webhooks/stripe`), tabla `suscripciones`, planes en `lib/facturacion/planes.ts` — **Esencial $147** (1 agente: chat, = el del contrato) y **Pro $297** (3 agentes: chat, voz y personalizado). **Período sin costo hasta el 30-sep-2026** (`FIN_CORTESIA`); nadie paga hasta activar el plan. Falta que Elvin dé `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` (hasta entonces la página dice "pagos no habilitados"). ⚠️ Pro promete agente de voz y uno personalizado que AÚN NO existen en la app.
**/uso rediseñado:** el cliente ve VALOR (reportes, cotizaciones, preguntas, personas, tiempo ahorrado estimado, actividad por día, estado del servicio); el costo de IA / tokens / latencia / jobs quedó en un **panel técnico interno** visible solo para `OPERADOR_EMAILS` (hoy elvin@levelupmediapr.net). Nunca mostrarle el costo de IA al cliente (margen).
**Techo de consumo (margen):** `lib/facturacion/limites.ts` — el consumo de IA no debe pasar del **30 % de la mensualidad** (objetivo 70 % de margen; crítico 35 % = margen mínimo 65 %). Al pasar el tope el agente entra solo en **modo económico**: reportes y cotizaciones bajan de Opus a Sonnet y el corpus grande (sitio web) sale del prompt. Las alertas de consumo van a AI Borinquen (`ALERTA_WEBHOOK_URL`, sirve un Incoming Webhook de Slack) — **antes le llegaban al cliente por WhatsApp, eso se quitó**. El margen del mes se ve en el panel técnico de /uso.
⚠️ **Postgres es compartido con Evolution**: el 6-sep /uso cayó con `53300 too many clients` (deploy con dos contenedores + Prisma). Arreglado con pool de 4 + timeouts en la app, `connection_limit=5` en Evolution y conteo de conexiones en `/api/salud`.
Falta: R2, dominio propio, máster real, escanear QR, claves de Stripe.

**Marca del cliente:** logo PLAGASPR.COM (casa roja + wordmark rojo/negro, tagline "Industrial · Comercial · Residencial"), rojo-naranja `#E63D1E`. Recreado en SVG en `public/logo*.svg` y `lib/marca/`; paleta de la app y de los PDFs en ese rojo. Si Gilbert manda el PNG original, reemplazar los SVG.

**Decisiones de Elvin:** repo nuevo + Railway; WhatsApp por **código QR** con "el canal más fácil" → Evolution (no oficial: número dedicado + lista blanca; adapter `lib/canales/` listo para Twilio); modelos mixtos; usuarios individuales por email.

**Dev local:** `node scripts/dev-db.mjs` (embedded-postgres :5433) · `node scripts/mock-evolution.mjs` (:8080, `/__simular/mensaje` con texto/imagenBase64/videoBase64) · launch.json `plagas` en :3100 · admin de prueba `elvin@levelupmediapr.net` / `plagas2026` · `.env.local` y Railway usan la ANTHROPIC_API_KEY dedicada del proyecto (Elvin la va a rotar después). ⚠️ Si cambian handlers/tools hay que **reiniciar el dev server** (instrumentation no recarga; en dev el runner y las rutas cargan módulos distintos, así que la memoria de ecos NO se comparte — por eso los envíos se guardan en base). Gotchas resueltos: `react-dom/server` prohibido en el grafo del servidor (las plantillas PDF son HTML puro); el modelo puede poner precio a una línea "pendiente" → el servidor fuerza `null` si no hay fuente. No hay `psql` en la Mac: consultar la DB local con `node -e` + `pg` y `DATABASE_URL` de `.env.local`.

**7-sep — reclamo de Kristian (amenaza de pausar y pedir devolución):** dice que le prometieron **acceso a internet ilimitado** y que lo necesita para **trabajar con su CRM, manejar y enviar facturas y reportes**. Respuesta técnica: se activó **acceso real a internet** ese mismo día (tools `web_search`/`web_fetch` del servidor de Anthropic en `lib/agente/loop.ts`, sin llaves extra, `WEB_MAX_BUSQUEDAS`/`WEB_MAX_LECTURAS` por costo; el agente cita fuentes y mantiene la jerarquía máster > internet). El verdadero hueco era el CRM + facturas → resuelto el 12-sep con el conector (abajo).

**10-sep — chat propio ("Tú") de WhatsApp:** el cliente conectó el número del negocio y quería hablarle al agente desde ese mismo teléfono (chat consigo mismo). Todo ahí llega con `fromMe=true` y el webhook lo descartaba. Arreglado: `esChatPropio` (fromMe + remoteJid == `payload.sender`) se acepta; el **eco** de lo que manda el agente se reconoce por id/texto en memoria (`esEcoPropio` en `lib/canales/evolution.ts`, números comparados solo por dígitos) y en base (`esEcoEnBase` en el webhook; TODO envío del agente —respuestas, tareas, `enviar_whatsapp`— se guarda como mensaje assistant con `id_externo` y el texto exacto). El número del negocio se autoriza solo ("Número del negocio", primer admin). Mock simula eco y fromMe.

**10-sep — pedidos de Kristian (memoria, navegar, Word/Excel, email):** hecho y desplegado. **Memoria** (`lib/memoria/`): tabla `memorias` (hecho/cliente/preferencia/pendiente), extracción automática tras cada turno (`lib/memoria/extraer.ts`, Sonnet effort low, dedupe por texto), tools `recordar`/`olvidar`, bloque MEMORIA DEL EQUIPO en el bloque volátil del prompt, página `/memoria`. **Exportar** (`lib/exportar/`): docx (librería `docx`, con fotos) y xlsx (`exceljs`, fórmulas) para reportes y cotizaciones; `/api/exportar/<tipo>/<id>?formato=`; tool `exportar_documento`; botones en el dashboard. **Correo** (`lib/email/`): SMTP por org cifrado en `credenciales_externas` (sistema `smtp`), `nodemailer`, log en `emails_enviados`, tool `enviar_email` que exige confirmación (`confirmado=true`) y no inventa direcciones; página `/correo` (presets Gmail app password / Outlook / otro, prueba de envío). Gilbert debe conectar plagaspuertorico@gmail.com con contraseña de aplicación.

**Chat del asistente anterior (export WhatsApp 11-jul → 11-ago-2026, analizado 10-sep con `scripts/analizar-chat-anterior.mjs`):** el asistente se llamaba **Zaida**, vivía en su propio número de WhatsApp dentro de un grupo "Plagas PR AI Assistant" con **Japhet** (escribe largo, define procesos, SOP) y **Kristian** ("christian plaga", manda fotos/videos y pide reportes). Era un agente tipo Claude Code con skills. **El CRM es PestForceApp**. Usos: reportes de hallazgo MIP con fotos/videos (formato "Reporte de Hallazgo y Servicio / MIP-CAPA"), propuesta+cotización+factura en PDF con letterhead (cliente Nelipak, Supremos PR, Casa Campo), reporte diario 3 PM de servicios del día siguiente por empleado con costos (desde el CRM), análisis de clientes/facturas del CRM, desarrollo del SOP/MSOP, benchmarking de competencia, descripción del programa MIP para facturas.

Incorporado al agente (10-sep): documento "Cómo trabaja el equipo con el asistente" cargado en prod, 27 memorias vía `POST /api/memoria/importar`, y hábitos en el prompt (confirmar al recibir, [Certain]/[Likely]/[Guessing] al identificar plagas, nombre y apellido, "evidencia fotográfica", CAPA, MIP/POE, sin bullets en POE). ⚠️ **El máster de PRUEBA tiene precios ficticios que contradicen los reales del chat** (residencial real: $50/mes hasta 2,500 pies², +$15 hasta 3,500; comercial base ~$89.99-95; industrial MIP personalizado). Hay que reemplazarlo. Agentes de voz (BLIPO/Twilio 787-498-0260) = plan Pro.

**12-sep — queja de Kristian "el robot no sigue mis comandos":** causa real = **Evolution se quedó sin memoria (OOM) el 11-sep 14:09**, reinició y la sesión de WhatsApp quedó en conflicto (`stream errored conflict type=replaced`, `sendText → 500 Connection Closed`). Arreglos: `NODE_OPTIONS=--max-old-space-size=3072` en evolution + reinicio limpio; responder_conversacion con 5 intentos y backoff hasta 10 min, sin marcar procesados si falla el canal, aviso a operadores; bug del runner (requeue chocaba con `jobs_agrupacion_unq` → 'cancelado' si hay job nuevo). **El número enlazado ahora es 787-587-2727** (el teléfono dedicado de Zaida; Japhet lo re-escaneó). Otros pedidos ya cubiertos: `enviar_whatsapp` desde el dashboard, `crear_documento_pdf` (documentos libres con marca), `crear_cotizacion` con `tipo='factura'`, `reportar_problema` + lista real de secciones en el prompt.

**12-sep — conector PestForceApp + tareas programadas ("como Zaida"):** Kristian pegó las credenciales del CRM en el chat de WhatsApp y Elvin autorizó usarlas. Construido y desplegado:
- **Sitios con acceso** (`lib/sitios/`): credenciales + `storageState` de Playwright cifrados en `credenciales_externas` (sistema `sitio:<host>`); login genérico (primer input de texto + password + Enter en `https://pestforceapp.com/`); `leerPagina` (texto/enlaces/JSON que carga la página, solo lectura). Tools `conectar_sitio` (admins; **borra la contraseña de todos los mensajes de la org**), `consultar_sitio`, `listar_sitios`. Página **/conexiones** (sitios + estado WhatsApp/correo).
- **PestForceApp** (`lib/sitios/pestforce.ts`): es una SPA que llama a `https://api.pestforceapp.com` con `Bearer <access_token>` (localStorage) y `job_center=59179115-24a4-4c8f-a5a0-b8cb1f4f70c7` (sale de `employee.job_center` en localStorage). Endpoints: `/administrative/events/` (servicios; **filtro real `event_initial_date`/`event_final_date`**, no `initial_date` que filtra por creación; sin `page_size` devuelve array), `/administrative/customers/?is_main=true&search=`, `/administrative/quotes/?search=`, `/administrative/catalogs/statuses/?module=EV|QO|TK`, `/administrative/notifications/`. Campos de evento: `initial_date, initial_hour, title, total, folio, employee[{name,job_title}], plague[{name}], customer{name,phone,address,contact_name}, status{name}, is_pay`. Tools `pfa_agenda` (por técnico con subtotales/total, texto listo para WhatsApp o json), `pfa_clientes`, `pfa_cotizaciones`, `pfa_api` (GET libre, admins). Si el token vence (401/403) relogea con Playwright. Usuario del CRM para el agente: `gpservicespr@gmail.com` (Japhet Alvarez; 2,163 clientes) — decirle a Kristian que cambie esa contraseña porque la pegó en el chat.
- **Tareas programadas** (`lib/tareas/`, tabla `tareas_programadas`, job `tarea_programada`, página **/tareas**): hora PR (`ahoraPR`), días, entrega por WhatsApp (destinatarios o números del creador) o email; el runner encola vencidas cada 60 s; tools `programar_tarea`/`listar_tareas`/`ejecutar_tarea_ahora`. Caso de uso: **reporte diario 3 PM de la ruta del día siguiente por técnico con costos** (viernes: sáb-lun). Probado en local end-to-end (lunes 14-sep: 16 servicios, $1,296.46).
- Scripts de exploración: `scripts/explorar-sitio.mjs`, `explorar-sitio-2.mjs`, `explorar-calendario.mjs` (Playwright, reciben la carpeta con `pfa.json`/`pfa-state.json`).

**14-sep — PDFs por el chat (queja de Kristian en Slack: "no genera documentos ni reportes"):** mandó `Reporte_Allied_Logistics_Z6.pdf` por WhatsApp como plantilla y el agente dijo que no podía abrir adjuntos del chat. Arreglado y desplegado: los adjuntos tipo documento (PDF/DOCX/TXT/MD) se extraen una vez (`lib/archivos/texto.ts`, cache en `archivos.meta.texto`) y van en línea en el historial (`<contenido_adjunto>`, 14k chars en el turno reciente); tools `leer_adjunto` y `guardar_adjunto_como_documento` (admins; crea/versiona en Documentos vía `lib/documentos/crear.ts`, compartido con la subida del dashboard). Regla en el prompt: nunca decir que no puede abrir adjuntos; preguntar en una línea si lo guarda como formato oficial. **Salidas del sistema** ("Entendido, estoy trabajando…", aviso de desconexión, entregas de tareas) ahora se guardan como mensajes assistant con `meta.sistema=true` (`lib/canales/registro.ts`): el eco del chat propio se reconoce en base y `historial.ts` los oculta al modelo. En dev el runner y las rutas no comparten memoria, por eso el eco en memoria no bastaba. El mock simula `documentMessage` (`documentoBase64`, `nombreArchivo`).

**15-sep — "sigo sin poder subir los reportes" (video de Nelipak por WhatsApp nunca llegaba):** causa en logs de Railway: `Request body exceeded 10MB for /api/webhooks/evolution`. Con `proxy.ts` presente, Next 16 bufferea el body de cada request con tope 10 MB (`experimental.proxyClientMaxBodySize`); los videos llegan en base64 dentro del webhook → JSON cortado → 400 → Evolution reintenta y el mensaje se pierde. Arreglado y desplegado: límite a 150 MB + `rescatarPayloadTruncado` en `lib/canales/evolution.ts` (recupera instance/key/pushName/tipo/caption del prefijo y deja el media pendiente → job `descargar_media` con `getBase64FromMediaMessage`); si `sender` se perdió, el chat propio se detecta con `instancias_canal.telefonoConectado`. Test `tests/canales/evolution.truncado.test.ts`; el mock devuelve un video real con `MOCK_VIDEO=<ruta.mp4>`. Lección: revisar `npx railway logs --service app` ante "no le llega X" antes de suponer que es el modelo.

**17-sep — reunión con Kristian/Japhet (8 pedidos) → construido y desplegado:** (1) Videos: fotogramas ya existían; ahora también **transcripción de audio** de videos y notas de voz (`lib/archivos/transcribir.ts`, ffmpeg→mp3 → Whisper con `OPENAI_API_KEY` o ElevenLabs Scribe con `ELEVENLABS_API_KEY`; cache en `archivos.meta.transcripcion`; sin llave el agente avisa "transcripción no activada"). ⚠️ Falta que Elvin ponga una de las dos llaves en Railway. (2) Gmail: guía paso a paso en /correo (myaccount.google.com/apppasswords) y en el prompt (GMAIL) para que el agente lo explique; OAuth de Google sería alternativa pero requiere proyecto en Google Cloud. (3) **Formato de documentos por chat**: `lib/reportes/estilo.ts` (`organizaciones.config.estiloDocumentos`: logo posición/tamaño, color, fuente, listas bullets/párrafos, firma, pie, títulos, `instrucciones` de redacción que se inyectan en los prompts de reporte/cotización); tools `ver_formato_documentos`, `ajustar_formato_documentos` (admin), `regenerar_pdf`; aplica a reporte, cotización/factura y documento libre. Verificado (logo derecha grande OK). (4) Velocidad: ráfaga de WhatsApp ahora 3 s para texto (`RAFAGA_TEXTO_MS`) y 15 s con media (`RAFAGA_MS`), antes 20 s siempre; pregunta simple ≈ 10 s end-to-end. (5) CRM masivo: `pfa_exportar` (`exportarTodo` en `lib/sitios/pestforce.ts`: page_size 200, 5 en paralelo, presupuesto 110 s con `parcial`, `aplanar` a columnas) → Excel + `resumen` (agrupar/sumar); medido: quotes 100/pág ≈ 3.5 s, customers 500/pág ≈ 12 s; filtros `initial_date/final_date` en quotes. Regla: contar = `pfa_api page_size=1` → `total`. (6) Excel genérico: `crear_excel` (`lib/exportar/tabla.ts`) + regla "nunca digas que no puedes generar Excel". (7) Video perdido: ver 15-sep (límite 10 MB) — pendiente confirmar con el chat real vía el endpoint de soporte. (8) Avisar a Japhet por punto: proceso manual de Elvin. **Endpoint de soporte**: `GET /api/operador/conversaciones` (header `x-operador-token` = `OPERADOR_TOKEN`, ya puesto en Railway; token en scratchpad de la sesión, regenerable con `railway variables --set`) para leer conversaciones/mensajes/jobs de prod sin login. SSE del chat web ya no revienta si el navegador cierra ("Controller is already closed").

**17-sep (tarde) — lo que mostraron las conversaciones reales de prod (vía endpoint de soporte):** los videos del 14-sep nunca llegaron (límite 10 MB, ya arreglado; el del 16-sep sí llegó y salió el reporte). Lo que Kristian pidió después: la foto del roedor sacada del video dentro del PDF → ahora `crear_reporte` guarda los fotogramas como archivos imagen (meta `deVideo`, cache `fotogramas` en el video) con id citable y entran al PDF (verificado). Japhet perdía 10-30 min porque el modelo usaba el **sandbox de código de web_search_20260209** (`code_execution`, `bash_code_execution`) para "procesar datos" y paginaba con `pfa_api` (20k chars ≈ 10 registros) → se cambió a `web_search_20250305`/`web_fetch_20250910` (sin sandbox), 8 iteraciones y una llamada final sin tools para que nunca responda vacío. Tool `modificar_tarea` (pausar/reanudar/borrar/editar). `pfa_exportar` con `filtro` (is_pay=false → balance pendiente). `mostrar_notas` en el formato (Kristian pidió quitar "NOTAS"). Adjuntos xlsx/csv legibles en el chat. Endpoint de soporte: `?memorias=1`, `?tareas=1`, `DELETE ?memoria=|?tarea=`; borré 16 memorias obsoletas en prod (límite de 10 registros, "no puede ver video", lentitud, número no autorizado…). Tareas en prod: 2 (3 PM y 6 PM "refresh", ambas a Kristian 17875474344 y Japhet 19394160033) — no son duplicadas. ⚠️ El modelo Opus se niega a citar fotogramas sintéticos (test card) como evidencia: para probar reportes con video hay que usar footage real. Carilin Sofia (COO, Slack U07V7MVJ18B) recibe el resumen de cambios por DM cuando Elvin lo pide.

**17-sep — transcripción ACTIVA en prod:** Elvin dio la `OPENAI_API_KEY` (proyecto OpenAI, Whisper `whisper-1`); está en Railway (`app`) y en `.env.local`. Probado local end-to-end: video con voz por WhatsApp → el agente repite lo que dice el técnico. Costo ≈ $0.006/min de audio. ⚠️ Railway mostró "subscription is past due" el 17-sep: si no pagan, apagan app/Evolution/Postgres.

**24-sep — PDF "Limitaciones Técnicas Actuales" (Japhet, 3 puntos) → resuelto y desplegado:** (1) **Llenar formularios PDF existentes**: `lib/pdf/formularios.ts` (pdf-lib + `extractTextItems` de unpdf): AcroForm por nombre (aplana) o PDF de imprenta — detecta blancos `____`, etiquetas cortas "Campo:" con espacio libre (≤5 palabras, mayúscula, un solo ":"), y el agente escribe también con `extras`: `marcar` (casillas ☐), `despues_de`, `debajo_de` (p. ej. "Nombre en letra de molde") y tabla `fila`×`columna` (Control de Activos). Tools `analizar_formulario_pdf` → `llenar_formulario_pdf` (devuelve `espacios_sin_llenar` para autocorregirse; cache `archivos.meta.formulario` v2). Probado con los formularios REALES de onboarding (Acuse, Control de Entrega de Activos, Acuerdo de Contratación): quedan bien. Los 10 de onboarding en prod no tienen AcroForm. (2) **Enviar documentos de la biblioteca**: tool `enviar_documento` (ids o nombres, al chat actual o a un contacto) + `enviar_whatsapp` acepta `documento_id` y ya funciona desde WhatsApp también. (3) **Números no autorizados**: `lib/canales/destinos.ts` + `organizaciones.config.contactosWhatsapp`; envío por nombre; admins pueden mandar a un número nuevo (se guarda). Contactos en prod: Kristian Torres 17875474344, Jean Torres 17876790343. Soporte nuevo: `?documentos=1`, `?analizar=<docId>`, `?descargar=<docId>`, `?contactos=1`, `POST {accion:'contacto',nombre,numero}`. Borradas 14 memorias obsoletas más ("no puede rellenar", "no autorizado", tickets de Carilin). El repo ya se sube a GitHub (`git push origin main`).

**Pendientes del cliente:** documento máster real (SOP/precios). **Pendientes de Elvin:** claves de Stripe, `ALERTA_WEBHOOK_URL`, cargar el máster real, opcional R2 y dominio propio. API key dedicada ya configurada (Elvin dijo que la rota después).

Ver [[bori-superplataforma]], [[voz-espanol-pr-tuteo]], [[elvin-aib-core-vision]].
